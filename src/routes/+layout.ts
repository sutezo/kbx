// Root layout options: this app is a pure client-side SPA.
// Nothing may run on a server — all crypto happens in the browser.
export const ssr = false;
export const prerender = false;
export const csr = true;
