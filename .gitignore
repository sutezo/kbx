node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Tools
.serena
# Claude Code: track project skills, but never commit local settings
# (they record machine-local paths and permission history)
.claude/settings.local.json

# Local scratch (may contain exported vaults — never commit)
/tmp/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
